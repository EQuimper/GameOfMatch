import React from 'react';
import LeftNav from 'material-ui/lib/left-nav';

export const MenuLeft = () => {
	<LeftNav open={this.state.open}>
		<MenuItem>My connection</MenuItem>
		<MenuItem>My Message</MenuItem>
	</LeftNav>;
};
